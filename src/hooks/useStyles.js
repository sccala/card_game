import { makeStyles } from '@material-ui/styles'

export const useStyles = makeStyles({
  playArea: {
    margin: '50px 25px',
  },
  messageArea: {
    margin: '10px',
  },
  message: {
    fontSize: '22px',
    color: 'white',
  },
})
